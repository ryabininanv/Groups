<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Groups"
        tools:targetApi="34">
        <activity
            android:name=".activity.authorization.AuthorizationActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.group.GroupActivity"/>
        <activity android:name=".activity.group.GroupListActivity"/>
        <activity android:name=".activity.group.AddGroupActivity"/>

        <activity android:name=".activity.form_education.FormListActivity"/>
        <activity android:name=".activity.form_education.FormActivity"/>
        <activity android:name=".activity.form_education.AddFormActivity"/>

        <activity android:name=".activity.speciality.SpecialityListActivity"/>
        <activity android:name=".activity.speciality.SpecialityActivity"/>
        <activity android:name=".activity.speciality.AddSpecialityActivity"/>

        <activity android:name=".activity.qualification.QualificationListActivity"/>
        <activity android:name=".activity.qualification.AddQualificationActivity"/>
        <activity android:name=".activity.qualification.QualificationActivity"/>
    </application>

</manifest>